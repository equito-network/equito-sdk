{
  "name": "equito-sdk",
  "version": "0.0.1",
  "scripts": {
    "build": "pnpm -r build",
    "clean": "pnpm -r clean",
    "lint": "pnpm -r lint",
    "prepack": "pnpm -r prepack",
    "docs:gen": "pnpm -r docs:gen"
  }
}
