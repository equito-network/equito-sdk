{
  "author": "Equito Network",
  "version": "0.1.0",
  "homepage": "https://github.com/equito-network/equito-sdk#readme",
  "bugs": "https://github.com/equito-network/equito-sdk/issues",
  "packageManager": "pnpm@9.4.0",
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "build": "pnpm -r build",
    "clean": "pnpm -r clean",
    "lint": "pnpm -r lint",
    "prepack": "pnpm -r prepack",
    "doc": "typedoc --plugin typedoc-plugin-extras --favicon assets/favicon.ico"
  },
  "devDependencies": {
    "typedoc": "^0.26.3",
    "typedoc-plugin-extras": "^3.1.0",
    "typescript": "^5.5.2"
  }
}
